name: FOMO_ONNX_Block
type: object-detection 
description: Custom block using ONNX model exported from PyTorch
parameters:
  epochs: 10
  learning_rate: 0.001
  batch_size: 32
  model_path:
    type: string
    description: Path to the ONNX model file
    default: model.onnx

